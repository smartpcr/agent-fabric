give me a modern looking workflow design UI where I can drop and drop components to canvas, the workflow can be saved, deleted, imported, exported, execution of workflow include run, cancel, pause, resume, failed and cancelled workflow can be rerun (start from beginning) or resume (start from step where it's failed/cancelled). The workflow should support sequential and parallel execution of its components, in addition to regular node, there are if/else/switch decision component, where i can drop additional components to its decision edge. There are also loop components, there are collapsible container components, and subflow components that reference another workflow. There are AI component, where it receives inputs (defined by skill or subagent), and config settings include how to pass input, output, etc. All components have inbound port (excluding start/trigger) that accepts input, and outbound port (onFinish) or error (onError) that generate output/errors
