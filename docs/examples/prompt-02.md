if-else, switch node should allow drag-drop component to its edge and put inside parent node, i.e. drag httpReq to if edge, drag log to else edge, if-else should contain both component, and their output should point to output of if-else, same is true for switch, loop, container component, we should allow node being dropped inside parent node, and the dropped node should be contained inside parent node, and its input/output should be connected to parent node's input/output, e.g. drag httpReq to container, httpReq's input should be connected to container's input port, and httpReq's output should be connected to container's output port, if there are multiple nodes being dropped, we can show a prompt to ask user whether they want to connect all nodes' input/output to parent node's input/output, or just keep them unconnected, or connect some of them, etc.
